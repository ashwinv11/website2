---
title: Ashwin Vaswani - Projects
layout: projects
---

- if paginate && num_pages > 1
  %p
    Page #{page_number} of #{num_pages}
  - if prev_page
    %p= link_to t("project.prev_page"), prev_page

.flex-boxes
  - page_articles.each_with_index do |article, i|
    .flex-box
      %h2
        = link_to article.title, article
        %span= article.date.strftime('%b %e, %Y')
      = article.body

- if paginate
  - if next_page
    %p= link_to t("project.next_page"), next_page
